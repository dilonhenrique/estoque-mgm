import { InputProps } from "@nextui-org/react";

export const globalConfig = {
  input: {
    variant: "flat" as InputProps["variant"],
    labelPlacement: "inside" as InputProps["labelPlacement"],
  },
};
